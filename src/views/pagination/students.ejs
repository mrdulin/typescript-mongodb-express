<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Students</title>
</head>
<body>
    <br />
    <div>
    <% if (!isFirstPage) { %>
        <span class="prepage"><a title="上一页" href="?page=<%= page-1 %>">上一页</a></span>
    <% } %>

    <% if (!isLastPage) { %>
        <span class="nextpage"><a title="下一页" href="?page=<%= page+1 %>">下一页</a></span>
    <% } %>
    </div>
</body>
</html>